<div class="container-fluid" style="background-color: #465362">
  <div class="holder">
    <div>
      <div>
        <h1>Welcome</h1>
      </div>
      <% if @scrape.id %>
        Hello
      <% end  %>
      <%= raise %>
    </div>
    <div class="submission">
      <%= render 'shared/submission' %>
    </div>
  </div>
</div>
<%# Colour doesn't work, need to move it to the bootstrap class or something %>
